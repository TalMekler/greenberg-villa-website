# Copy to .env. These create the FIRST admin account on first run; afterwards
# server/data/users.json is the source of truth and these are ignored.
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD=change-me

# Optional — the booking API's port (default 3001).
# API_PORT=3001
