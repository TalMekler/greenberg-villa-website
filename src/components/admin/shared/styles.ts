/** Field and label classes shared by every admin form. */
export const adminFieldClass =
  "w-full rounded-[4px] border border-field bg-white px-4 py-3 font-sans text-[15px] text-ink transition-colors focus:border-navy";

export const adminLabelClass =
  "font-sans text-[12px] font-bold tracking-[0.06em] text-slate uppercase";

/** Standard admin button, used for row actions and panel submits. */
export const actionButton =
  "rounded-[4px] px-3 py-2 font-sans text-[12px] font-bold tracking-[0.04em] uppercase transition-colors disabled:cursor-not-allowed disabled:opacity-40";

/** A tighter variant for controls sitting inside a list row. */
export const smallButton =
  "rounded-[4px] px-3 py-1.5 font-sans text-[11px] font-bold tracking-[0.04em] uppercase transition-colors disabled:cursor-not-allowed disabled:opacity-40";
