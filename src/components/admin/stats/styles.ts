/**
 * The stats panel packs more labels into less space than the other admin
 * sections, so it runs its own slightly smaller label — 11px, not the shared 12.
 */
export const statsLabelClass =
  "font-sans text-[11px] font-bold tracking-[0.06em] text-slate uppercase";
